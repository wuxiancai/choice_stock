# Handoff

## 当前状态

第一版已实现：可本地启动、可由 systemd 托管的 A 股盘后选股与行业资金流看板。`deploy.sh` 会自动检测 Python 3.12+；macOS 缺少时通过 Homebrew 安装，Ubuntu 缺少时通过 apt（必要时 deadsnakes PPA）安装，再创建 `.venv` 与安装全部项目依赖。

`20260815` 技术信号筛选新增股票代码条件：可输入 6 位代码或完整 Tushare 代码（如 `000001`、`000001.SZ`），会按 `ts_code` 前缀筛选，并与各数值区间条件叠加生效。

`20260815` 首页 UI 已按截图收紧：移除标题下的研究用途/定时说明，并把“立即同步”移入数据健康状态标题行；行业表仅允许拖动右侧滚动条，禁止滚轮滚动，排名不再带 `#`；技术信号筛选在桌面端使用自适应八列单行布局、窄屏换行；当日技术信号中“九转”为红色，“净流入”替换原主力净流入说明。

`20260815` 技术信号筛选中的资金流标签也统一为“净流入”；此前只改了当日技术信号表头，造成两处文案不一致。

`20260815` 已删除首页及浏览器标题中的“A股盘后选股”；数据健康状态卡现在为页面首个区块。

`20260815` 当日技术信号默认按“净流入”从高到低排序，缺失净流入固定在末尾；同额时再按评分从高到低排序。表头以向下箭头标识默认顺序，点击仍可切换排序。

`20260815` 当日技术信号的 PE 已紧随“净流入”列展示，并明确标为 `PE TTM`。Tushare `daily_basic` 不再读取静态 `pe`，而是读取并保存 `pe_ttm`（动态市盈率）；筛选中的 PE 同样使用该值。下次同步会更新最新交易日的已有 PE 值。

`20260815` 当日技术信号在“评分”前新增“行业”列，使用 Tushare `stock_basic.industry` 的股票行业分类。行业与信号同表持久化，数据缺失时显示 `—`。

`20260815` 当日技术信号将量比、换手率移动至 MACD 前；筛选的“股票代码/名称”输入支持按代码或股票名称前缀匹配；筛选区“九转”标签使用红色。

`20260815` 技术信号新增 BBI(3/6/12/24)、BIAS(6)、VR(26)、PSY(12) 与 DMI(ADX14)，均可在当日表排序和用最小/最大值筛选。VR 依赖成交量，DMI 需要至少 28 根日线；窗口不足或无法计算时保持 `NULL`，页面显示 `—`。既有历史信号将在下一次同步重算后填充。

技术信号表现已覆盖量比、换手率、成交额、总市值、PE、PB 与涨跌幅；筛选仅保留评分、九转、主力净流入、量比、换手率、成交额、总市值、PE、PB 的最小/最大值组合。MACD、KDJ、RSI、布林位置和涨跌幅仍展示及支持表头排序，但不再作为筛选条件。估值与量比来自 Tushare `daily_basic`；当前数据库中的历史信号需在下一次同步后重新计算才会填充新增字段，数据源无权限或缺值时页面显示 `—`。

`20260814` 修复了新增字段迁移后旧信号的 `pct_chg=NULL` 导致首页模板比较 `NULL >= 0` 并返回 500 的问题；页面会把该类历史缺值显示为 `—`。

`20260814` 行业板块固定标为最近 5 个交易日，并按每个交易日的涨跌幅增加名次；主力净流入使用红色、净流出使用绿色，金额统一显示为 `亿` 或 `万`。当日技术信号中的成交额（Tushare 千元）、总市值（Tushare 万元）和主力净流入（元）均已按实际人民币金额格式化；九转改为正数 Sell、负数 Buy，只有第 8/9 转才显示对应的红/绿信号。历史板块数据仍必须由支持指定交易日的数据源真实回补，当前公开降级源仅能提供当日数据，不能伪造为历史快照。

`20260814` 修复行业五日矩阵仅有最新日快照的缺陷：首次同步会在写入当日板块快照后，逐行业通过东方财富历史资金流与历史行情接口回填缺失的前四个交易日；历史接口缺失时不复制当日数据。日期列显示每日涨跌幅排名；“5 日净流入/涨幅”改为五日数据的算术平均值，并且只有五天完整快照时展示。技术信号的涨跌幅、主力净流入已移至 MACD 前。行业板块和当日技术信号表均默认显示 10 行，保留表头并在表内滚动查看其余结果。

`20260814` 若 Tushare/东方财富探测失败但备用 `ths` 已返回完整行业数据，同步仍视为成功；实际数据源记录在 `sync_runs.source`，前置探测失败不再显示为用户可见错误。行业矩阵的五个表头改为根据已同步日线的最近五个交易日生成，因此即使板块历史回填尚未完成，也会固定显示五个日期，并以 `—` 明确标出缺失排名。

`20260815` 行业矩阵按最新交易日涨幅从高到低排序，第二、三列固定为当日涨幅和当日净流入；日期列保留对应交易日的涨幅排名。九转展示不再隐藏第 1–7 转，因此筛选区间 `4–5` 的结果会显示 `4` 或 `5`。东方财富历史回填先单次预热行业代码映射、再限为 3 路并发，避免并发重复映射请求造成 502；若历史接口仍不可用，只向页面显示一条包含失败行业数量的摘要，而不展示逐行业 URL 错误。当日 `ths` 数据已成功时不受历史回填失败影响。

`20260815` 所有页面日期统一为 `YYYY-MM-DD`，日志时间统一显示为上海时区 `YYYY-MM-DD HH:MM:SS`。九转列仅显示数字：正数（上涨/Sell）红色，负数（下跌/Buy）绿色并以绝对值显示。行业资金流真实备用顺序为 Tushare 同花顺、Tushare 东方财富、东方财富、同花顺、腾讯。腾讯源已接入：拉取全市场实时股票的涨跌幅与主力净流入，并按 Tushare 股票行业分类加权聚合成当日行业快照；它不能替代东方财富的历史行业回填。新浪未接入。所有来源失败时错误文案会压缩为每个来源的简短状态，不显示权限 URL 或原始响应。

`20260815` 修复“立即同步”未补齐历史行业快照的问题：此前只要某日期存在任意一条板块记录便跳过，残缺的历史日期因而不会重试；现在日线与行业快照均以有效行数判定完整，缺失或残缺日期才会被增量回补，完整历史不会重复拉取。页面的“实际已同步”也只计完整快照日。技术信号筛选移除评分、总市值（表格仍展示）；九转列移至评分后第 3 列；标题和数据健康状态在桌面端同一行，窄屏自动纵向排列。

`20260815` 行业同步状态改为可审计：当日区块会展示成功来源、已尝试但失败的来源及因前序成功而未尝试的来源。历史回填则先逐交易日尝试 Tushare 同花顺、Tushare 东方财富；未回填日期才走东方财富行业历史接口，并显示每源成功/失败摘要。AKShare 同花顺、腾讯只支持当日快照，会明确显示为“历史回填未使用”，绝不把即时数据伪造为历史。交易日始终来自 Tushare `trade_cal(exchange=SSE, is_open=1)`，不会按自然日拉取休市日。

`20260815` 按账户 3000 积分权限审计并移除了 `moneyflow_ind_ths`、`moneyflow_ind_dc` 的所有调用、历史回填与状态文案。这两个行业资金流接口不再产生无权限错误；保留的 Tushare 调用为 `trade_cal`、`daily`、`daily_basic`、`moneyflow`、`stock_basic`，均不高于 3000 积分门槛。当日行业数据按东方财富 → 同花顺 → 腾讯的公开源降级；历史行业快照仅使用东方财富公开历史接口。测试已覆盖越权端点不存在且全套 `25 passed`。

`20260815` 修复个股及 Tushare 行业聚合的主力净流入口径：此前错误地只使用特大单净额，并把 Tushare `moneyflow` 的“万元”错误换算为 `*1000`。现在统一使用 `(大单买入-大单卖出)+(特大单买入-特大单卖出)`，并乘 `10000` 存储为人民币元；页面和筛选器明确标注“大单+特大单”。重新同步最新交易日会覆盖旧值并重算信号。Tushare 的 `net_mf_amount` 是全口径主动买卖净额，不作为“主力净流入”。

`20260815` 修复东方财富历史行业数据 `0/110`：AKShare 的行业名称映射请求在 FastLink 代理下被重置，导致真正的历史请求尚未发出。项目改为使用带浏览器请求头、代理/直连重试的东方财富 JSON 与板块代码接口，保留它作为备用；同时启用 3000 分权限内的 Tushare `moneyflow` 个股资金流，按 `stock_basic` 行业及 `daily` 成交额加权聚合为历史和当日行业快照。真实同步已写入 `20260810` 至 `20260814` 各 110 行 `tushare_moneyflow_aggregate`，完整五日行业数为 110，最近一次运行 `success`。同花顺与腾讯仍只作当日公开源备用。

`20260815` 页面截图出现的行业矩阵 `—` 已核对为旧 Web 进程/页面停留在修复前的 440 条板块状态，并非当前 SQLite 缺行。重启 `start.sh` 后，HTTP 页面实际返回“板块 110 条、实际已同步 5/5 个交易日”；电器仪表、矿物制品、通信设备、运输设备、玻璃等均已有五日排名和均值。若浏览器仍显示旧值，强制刷新页面。

页面最后新增“系统日志”：`sync_latest` 失败、行业资金流降级和页面/API 未处理异常会写入 SQLite `system_logs`，展示最近 50 条 ERROR（UTC 时间）；记录中的 Tushare Token 会脱敏。

当日技术信号表支持浏览器端点击表头排序（再次点击反向；`—` 缺失值始终在末尾），不改变后端筛选或需要重新同步。

## 下一步

1. 已真实回补：119 个交易日，最新 `20260813`，当日 5,540 条日线，生成 5,521 条技术信号；页面可展示前 30 条。
2. 行业资金流盘后优先使用 Tushare `moneyflow` 聚合（3000 分内），失败时才降级东方财富（带代码直连与重试）→ 同花顺 → 腾讯；禁止探测 Tushare 行业资金流接口。当前本机真实数据库最近五日均为 110 行，仍需在云服务器重复同步确认其网络可达性。
3. 打开 `http://127.0.0.1:8012`，等待 21:00 任务或按需点击“立即同步”。

个股主力资金走 Tushare `moneyflow`；需以首次真实同步确认当前积分权限。龙虎榜仍是下一切片，不能声称已接入。

`start.sh` 的启动提示中，变量后紧接中文标点必须使用 `${VAR}`；在 `set -u` 环境下裸 `$VAR` 可能被 shell 解析为包含全角符号的变量名。

首次点击“立即同步”会从 Tushare 最近 120 个开市日候选中回补至少 90 个实际有日线的交易日；已有日线只补缺失日期，并每次刷新最新日。交易日历已开市但日线尚未发布时只跳过该日期，不使整批同步失败。行业资金流仍依赖东方财富可访问性，代理断连时应保持 `partial` 状态。
