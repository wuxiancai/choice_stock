# Handoff

## 当前状态

第一版已实现：可本地启动、可由 systemd 托管的 A 股盘后选股与行业资金流看板。尚未用目标环境的真实 Token 完成首次同步。Mac 开发机只有 Python 3.9，项目部署脚本会强制要求 3.12+，本机无法做运行态部署验收。

## 下一步

1. 在目标 macOS/Ubuntu 上执行 `bash deploy.sh`。
2. 将 Token 写入仅本地存在的 `.env`，执行 `./start.sh`。
3. 打开 `http://127.0.0.1:8012`，点击“立即同步”或等待 21:00 任务，并检查数据健康状态。
